scheduler=spread           # base (used if no sweep.schedulers)
workload=poisson

num_tenants=3
num_workers=3
total_invocations=200
batch_size=10
time_step=5.0
seed=1                     # base (used if no sweep.seeds)

arrival_rate=0.5
idle_timeout=60            # base timeout

attacker.enabled=1
attacker.tenant=999
attacker.function=100
attacker.attack_per_victim=1.0

output_csv=results/sweep_poisson_attack.csv

# Sweeps:
sweep.schedulers=spread,random,helper
sweep.seeds=1,2,3
sweep.idle_timeouts=10,60
